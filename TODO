-ruler option
-grid over background fill but not images
-images over background fill
-variance in background fill within ranges
-default island on load
-show instructions
-fix passing around ctx
    -aka actually just pass it instead of re finding element evry time
-'are you sure' pop up on reset map click
-fix when mouse is dragged off canvas pan continues
-side pop outs on buttons (instead of down)
-select different tools for right and left click